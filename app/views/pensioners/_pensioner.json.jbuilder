json.extract! pensioner, :id, :turno, :numero_oficio, :fecha_oficio, :fecha_recepcion, :promovente, :numero_juicio, :turnado, :created_at, :updated_at
json.url pensioner_url(pensioner, format: :json)
